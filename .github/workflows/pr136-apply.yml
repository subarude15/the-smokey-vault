name: PR136 source patch

on:
  push:
    branches:
      - pr136-cocktail-cards
    paths:
      - .github/workflows/pr136-apply.yml

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR136 patch workflow is valid"
